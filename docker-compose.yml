services:
  frontend:
    build:
      context: ./Frontend
      dockerfile: Dockerfile
    environment:
      YII_ENV: "prod"
      YII_DEBUG: "0"
      # Beispiel für API-Basis-URL Richtung Backend:
      API_BASE_URL: "http://backend:9125"
    ports:
      - "8080:80"
    # Optional persistente Volumes nur für Laufzeitdaten:
    volumes:
      - app-runtime:/var/www/html/runtime
      - app-assets:/var/www/html/web/assets

volumes:
  app-runtime:
  app-assets:
